# Frontend local dev — copy to .env.local
VITE_API_URL=
# Leave empty in dev to use Vite proxy (/api → localhost:3001)

# Production — see .env.production.example (set VITE_API_URL in Vercel dashboard)
